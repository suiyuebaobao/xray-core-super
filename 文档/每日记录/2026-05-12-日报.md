# 2026-05-12 日报

## 节点运营中心 v1

- 新增节点运营中心方案：第一版只做观测和诊断，不自动停用节点、不隐藏订阅线路、不触发用户禁用。
- 新增迁移 `017_add_node_operations_center`，包含 `node_runtime_metrics` 与 `node_health_checks`。
- node-agent 心跳新增可选 `runtime_metric`，上报 CPU、内存、磁盘、load、TCP 连接数、Xray 进程状态和采集时间；旧 agent 不上报该字段仍兼容。
- worker 新增每分钟节点健康检查任务，综合心跳、TCP 端口、流量上报、运行指标和 Xray 状态生成健康分与故障原因。
- 管理后台新增 `/admin/node-operations`，展示健康计数、节点健康矩阵、运行负载和今日/月度节点流量排行。
- 新增管理接口 `GET /api/admin/node-operations/summary` 与 `GET /api/admin/node-operations/nodes/:id/checks`。

## 用户级限速 v1

- 新增迁移 `018_add_user_speed_limit`，在 `user_subscriptions` 增加 `speed_limit_bps`，`0` 表示不限速。
- 管理后台用户订阅弹窗新增“用户限速(Mbps)”输入，用户列表展示当前限速。
- `GET/PUT /api/admin/users/:id/subscription` 支持返回和保存 `speed_limit_bps`。
- 节点访问 `UPSERT_USER` 任务 payload 增加 `speed_limit_bps`，单出口和 multi_exit 都会下发给 node-agent。
- node-agent 增加本地限速策略和状态文件，基于 Xray Stats 做滑动窗口平均限速：超速时临时摘除对应 VLESS 用户，窗口结束后自动恢复。
- 限速不改变订阅下发、不改变套餐扣费和流量账本；中转访问仍由出口节点执行限速。

## 订阅配置 v1

- 新增后台 `/admin/subscription-settings`，支持维护订阅名称、Clash/mihomo 自定义规则、用量头开关、更新间隔和订阅页面地址。
- 新增管理接口 `GET/PUT /api/admin/site/subscription`，配置存储在 `site_settings.setting_key=subscription_config`，不新增数据库表。
- 订阅下载响应新增 `profile-title`、`profile-update-interval`、可选 `profile-web-page-url` 和 `subscription-userinfo`；用量头按普通流量池与家宽流量池合计已用、合计总量和到期时间生成。
- Clash/mihomo YAML 的 `rules` 改为后台可编辑；规则为空时回退默认规则，缺少兜底规则时自动追加 `MATCH,PROXY`。
- 订阅名称优先使用后台配置，未保存后台配置时回退 `SUBSCRIPTION_PROFILE_NAME`。

## 文档同步

- 已同步三份规则文件、开发方案、订阅接口、管理接口、前端页面清单、前端架构说明和生产部署指南。
- 文档继续使用 `<exit-node-host>`、`<primary-center-url>` 等占位符，不记录真实节点 IP、域名、Token、SSH 信息或 Reality 密钥。

## 生产与测试节点处理

- 已重新生成 node-agent Docker 镜像包，并升级启用中的出口节点 agent；当前启用 node host 均持续上报心跳和 `runtime_metric`。
- 测试出口节点避让 443 端口：TCP 线路改为自定义非 443 端口，XHTTP 线路继续使用独立端口；远端复核 443 由原有 Web 服务监听，RayPilot Xray 只监听本次配置的节点端口。
- 节点运营中心接口复核通过：返回 6 条启用线路，其中 6 条均带运行指标。

## 验证

- 已通过：`docker run --rm -v /root/suiyue:/app -w /app golang:1.25 gofmt ...`
- 已通过：`docker run --rm -v /root/suiyue:/app -w /app golang:1.25 go test ./...`
- 已通过：`cd frontend && npm run build`
- 已通过：`docker compose config --services`
- 已通过：`docker compose run --rm migrate`，执行 `17/u add_node_operations_center` 与 `18/u add_user_speed_limit`
- 已通过：`docker compose up -d --build frontend-build api worker nginx`，API/Worker/前端构建滚动更新，MySQL 未停止
- 已通过：`E2E_BASE_URL=http://127.0.0.1 E2E_ADMIN_USERNAME=admin E2E_ADMIN_PASSWORD=*** npm run test:e2e -- e2e/admin-user-smoke.spec.js --workers=1`
- 已通过：`E2E_BASE_URL=http://127.0.0.1 E2E_ADMIN_USERNAME=admin E2E_ADMIN_PASSWORD=*** npm run test:e2e -- e2e/admin-crud.spec.js --workers=1`，覆盖 `speed_limit_bps=2000000` 保存、订阅查询和用户列表返回
- 已通过：节点运营中心 API 实测，`/api/admin/node-operations/summary` 返回 6 条启用线路且运行指标完整
- 已通过：测试出口节点远端端口复核，443 未被 RayPilot Xray 占用
- 已通过：`git diff --check`
- 已通过：仓库文档和源码敏感信息扫描，未发现真实节点 IP、域名、SSH 密码、代理账号或 Token 残留
