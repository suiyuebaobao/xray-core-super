# 2026-05-05 日报

## 本次处理

- 真实执行 154.219.106.105 多出口 IP 一键部署，扫描到可用公网出口 154.219.106.105 和 154.219.106.53，跳过 Docker 私网地址。
- 部署为一个 `AGENT_ROLE=multi_exit` 的 `raypilot-node-agent` 容器，创建 `node_hosts.id=1` 与逻辑节点 33、34。
- 补强一键部署清理逻辑：部署前清理旧 systemd `node-agent`、旧 node-agent 二进制、旧出口 agent 容器、旧 relay agent 容器，并停止冲突的宿主机 Xray。
- 清理生产服务器旧 relay agent 容器，停用旧 relay/backend 记录，避免订阅继续下发不可用中转线路。
- 停用旧单节点记录 `nodes.id=3`，将节点 33/34 加入默认节点组并触发已有活跃订阅用户同步。

## 验证记录

- 目标服务器 systemd `node-agent` 为 inactive，旧 relay agent 容器已删除，仅保留 `raypilot-node-agent`。
- Xray 监听 154.219.106.105:443 与 154.219.106.53:443；`node_33_out` 和 `node_34_out` 的 `sendThrough` 分别绑定对应出口 IP。
- 节点 33/34 的 UPSERT_USER 任务均为 DONE，Xray clients 已写入。
- 订阅 `/sub/{token}/plain` 返回 200，包含节点 1、33、34 三条 VLESS 线路，不包含已停用中转线路。
- Xray 客户端真实出站验证通过：节点 33 返回 154.219.106.105，节点 34 返回 154.219.106.53。

## 追加多用户 E2E 验证

- 新增 `frontend/e2e/admin-ops-flow.spec.js`，覆盖管理后台真实 UI 路径：登录、Dashboard、出口节点新增/编辑、一键部署多 IP 开关、节点分组绑定、套餐管理、relay 与后端绑定、用户开通订阅、用量弹窗、订单、兑换码和订阅 Token 格式切换。
- 新增 `frontend/e2e/real-user-flows.spec.js`，使用两个独立浏览器上下文和两个 API actor 模拟用户 A/B：注册、登录、套餐浏览、下单、兑换码开通、订阅页、Clash/Base64/URI 下载、个人资料更新和用户数据隔离。
- 新增 `frontend/e2e/traffic-flow.spec.js`，覆盖 node-agent 数据流：心跳领取 UPSERT_USER、任务结果回传、订阅下载、单节点流量基线/增量/乱序旧批次/计数器归零/恢复增长、真实 multi_exit 两个逻辑节点流量上报、超额挂起和用量页面可见性。
- 完整 Playwright 使用 `--workers=1` 串行执行，避免同一管理员账号在多个测试文件中刷新登录态互相影响；多用户并发行为在用例内部通过多个 browser context 和 API actor 模拟。
- 测试后复查 `e2e-crud-*`、`e2e-ops-*`、`real-user-flow-*`、`traffic-flow-*`、`debug-task-*` 前缀，确认无活跃测试节点、节点分组、relay、活跃套餐或测试用户残留。

## UAT 复核与前端收口

- 针对手工 UAT 报告中“节点分组绑定刷新后未持久化”的疑点，前端保存绑定成功后改为强制重新拉取后端分组列表，避免仅依赖本地状态导致测试误判。
- 针对“兑换码有效性状态不一致”的疑点，后台生成兑换码后新增“本次生成的兑换码”展示区和复制按钮，避免测试人员误复制旧码或已使用码。
- 针对“个人中心保存后用户名曾为空”的疑点，保存资料成功后重新拉取 `/api/user/me` 并回填本地用户信息。
- 已将构建产物同步到公网 Nginx 实际静态目录 `/www/wwwroot/suiyue`。
- 公网 `http://156.238.231.216` 完整 Playwright 复核通过，5 个用例全部通过；复核后确认无测试节点、分组、relay、活跃套餐或测试用户残留。

## 文档同步

- 已同步更新 `AGENTS.md`、`CLAUDE.md`、`QWEN.md` 的测试节点信息，敏感字段统一脱敏。
- 已更新 `开发方案.md`、节点代理部署指南、管理接口文档、OpenAPI 摘要、联调记录、运维手册和常见问题排查手册。

## 收尾结果

- `go test ./...` 通过。
- `make build` 通过，并已重启 `suiyue-api.service`、`suiyue-worker.service`，`/health` 返回正常。
- `make node-agent-image` 通过，已重新生成 `/root/node-agent-image.tar.gz`。
- `cd frontend && npm run build` 通过。
- `cd frontend && E2E_ADMIN_USERNAME=*** E2E_ADMIN_PASSWORD=*** E2E_REQUIRE_MULTI_TRAFFIC=1 ... npm run test:e2e -- --workers=1` 通过，5 个 Playwright 用例全部通过。
- `cd frontend && E2E_BASE_URL=http://156.238.231.216 E2E_ADMIN_USERNAME=*** E2E_ADMIN_PASSWORD=*** npm run test:e2e -- --workers=1` 通过，5 个公网 Playwright 用例全部通过。
- Playwright 后再次复核生产节点、订阅和 Xray 客户端真实出站，结果仍正常。
