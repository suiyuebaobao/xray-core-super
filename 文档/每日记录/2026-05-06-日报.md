# 2026-05-06 日报

## 节点传输模式多选

- 管理后台「节点管理」新增和一键部署的传输模式从单选调整为多选，支持只选 TCP、只选 XHTTP，或同时选择 TCP 与 XHTTP。
- 同时选择 TCP 与 XHTTP 时，系统按多条逻辑线路处理：TCP 和 XHTTP 分别创建独立 `nodes` 记录、独立 inbound/outbound 和独立订阅线路。
- 同一 IP 的 TCP 与 XHTTP 必须使用不同端口，默认 TCP 443、XHTTP 8443，避免一个 Xray inbound 同时承载两种 network。
- 一键部署在多传输模式下使用 `AGENT_ROLE=multi_exit`，即使只有一个出口 IP，也只安装一个 node-agent，由 `MULTI_NODE_CONFIG` 管理多条逻辑节点。
- node-agent multi_exit 配置校验从“IP 唯一”调整为“IP:Port 唯一”，允许同一 IP 分别监听 TCP 和 XHTTP。
- 新增/更新文档：规则文件、`开发方案.md`、管理接口、订阅接口、节点代理部署指南、前端页面文档。
- README 脱敏截图已刷新，`admin-nodes.png` 展示 XHTTP 逻辑节点，`admin-node-multi-ip-deploy.png` 展示 TCP/XHTTP 多选与双端口。

## 验证记录

- 已通过：`go test ./...`
- 已通过：`cd frontend && npm run build`
- 已通过：`make build`
- 已通过：`docker-compose config --services`
- 已通过：`make node-agent-image`
- 已通过：使用新镜像内 Xray 26.3.27 对同 IP TCP 443 + XHTTP 8443 执行 `xray run -test`，返回 `Configuration OK`
- 已通过：公网 Playwright 全流程 5/5
- 已通过：强制 `E2E_REQUIRE_MULTI_TRAFFIC=1` 的 `traffic-flow`，临时 TCP/XHTTP 多逻辑节点夹具覆盖 `/api/agent/multi/traffic`，测试后夹具已清理
- 已发布：`suiyue-api`、`suiyue-worker` 已重启并保持 active，公网首页和 `/api/plans` 返回 200

## 新中转节点部署

- 新增一台中转服务器，后台 relay 记录已脱敏。
- 通过生产 API `POST /api/admin/relays/deploy` 部署 `raypilot-relay-agent`，运行模式为 `AGENT_ROLE=relay`。
- 中转容器内 HAProxy 版本为 3.2.16，配置校验通过。
- 已绑定并启用后端 `<relay-port> -> <exit-node-ip>:443`。
- 已验证：relay 心跳在线、中转监听端口 TCP 可达、订阅 URI 包含新中转线路。
- 本次交付复验通过：`go test ./...`、`cd frontend && npm run build`、公网 Playwright 全套 5/5、远端 HAProxy 配置校验和公网 TCP 连通性检查。
- 已同步更新 `AGENTS.md`、`CLAUDE.md`、`QWEN.md`；SSH 密码和 Relay Token 只记录为 `[REDACTED]`。

## 一键部署闭环增强

- 出口节点一键部署新增 `node_group_ids` 和 `replace_existing_role`：部署成功后自动绑定节点分组、对已有活跃订阅下发 `UPSERT_USER`，并可按同一物理服务器 `agent_base_url` 自动停用旧 relay/旧出口记录。
- 中转节点一键部署新增 `exit_node_id`、`listen_port`、`target_port`、`backend_name` 和 `replace_existing_role`：部署成功后自动创建 `relay_backends`、写入 `RELOAD_CONFIG` 并等待 HAProxy reload 成功。
- 后台部署表单已同步补齐上述选项，避免“agent 已部署但还要人工去后台补分组或补后端绑定”的半成品流程。

## 香港/美国角色互换

- 已通过真实一键部署把一台服务器切换为出口节点，节点记录和 Reality 公钥已脱敏保存于私有运维记录。
- 已通过真实一键部署把一台服务器切换为中转节点，relay 记录和后端绑定已脱敏。
- 自动停用旧角色已生效：旧出口节点 `1` 已停用，旧中转 `52` 已停用。
- 已验证：中转监听端口 TCP 可达，relay 心跳在线，出口节点心跳和流量上报在线，订阅 plain 已出现新中转线路且 Reality 参数来自出口节点。
- 已验证：公网 Playwright smoke 再次通过，线上首页已切到新前端构建 `index-Be9-UD8m.js`。

## 全协议出口与全中转线路

- 已把一台出口服务器扩成 `TCP + XHTTP` 两条出口节点。
- 已把双出口服务器扩成四条逻辑出口节点，分别覆盖两个出口 IP 的 `TCP + XHTTP`。
- 已把脱敏中转节点的后端绑定扩成多条线路，覆盖所有活跃出口和所有协议。
- 已验证：订阅 plain 现在同时包含所有直连出口线路和所有香港中转线路；XHTTP 线路使用 `type=xhttp`、`path=/raypilot`，且不下发 `flow`。
